---
description:
globs:
alwaysApply: false
---
# Regola Enum UserTypeEnum (STI Parental)

## Obiettivo
Gestire SEMPRE il tipo utente tramite enum PHP 8.1+ (UserTypeEnum), MAI tramite costanti stringa o integer.

## Definizione
```php
namespace Modules\SaluteOra\Enums;

use Filament\Support\Contracts\HasLabel;

enum UserTypeEnum: string implements HasLabel
{
    case ADMIN = 'admin';
    case DOCTOR = 'doctor';
    case PATIENT = 'patient';

    public function getLabel(): string
    {
        return match($this) {
            self::ADMIN => 'Amministratore',
            self::DOCTOR => 'Dottore',
            self::PATIENT => 'Paziente',
        };
    }
}
```

## Regole
- Usare SEMPRE l'enum UserTypeEnum per il campo `type` nei modelli User/Patient/Doctor/Admin (STI Parental)
- Cast automatico Eloquent:
  ```php
  protected $casts = [
      'type' => UserTypeEnum::class,
  ];
  ```
- In Filament, usare l'enum per:
  - Opzioni Select, Radio, CheckboxList
  - Colonne Table/Badge (label e colore)
  - Policies e scoping
- MAI usare costanti tipo `public const TYPE_ADMIN = 'admin'` (deprecato)
- Documentare sempre l'uso dell'enum in ogni modulo che gestisce utenti

## Best Practice Filament
- Implementare HasLabel per label UI
- Usare l'enum direttamente in Select/Radio/Badge:
  ```php
  Select::make('type')->options(UserTypeEnum::class)
  ```
- Vedi: [Filament Enums Docs](mdc:https:/filamentphp.com/docs/3.x/support/enums)

## Collegamenti
- [SINGLE_TABLE_INHERITANCE.md](mdc:../SINGLE_TABLE_INHERITANCE.md)
- [filament-admin-service-provider-multi-tenancy.md](mdc:../filament-admin-service-provider-multi-tenancy.md)
- [ENUMS_BEST_PRACTICES.md](mdc:ENUMS_BEST_PRACTICES.md)
- [Filament Enums](mdc:https:/filamentphp.com/docs/3.x/support/enums)

---
**Questa regola è OBBLIGATORIA per tutti i moduli che gestiscono utenti con STI.**
